<Window x:Class="ProcessMonitor.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProcessMonitor"
        xmlns:vm="clr-namespace:ProcessMonitor.ViewModels"
        mc:Ignorable="d"
        Title="Process Monitor" Height="600" Width="800">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.ColumnSpan="3" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Filtruj:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" Width="150" Margin="0,0,20,0"/>

            <Button Content="Odśwież" Command="{Binding RefreshCommand}" Padding="10,2" Margin="0,0,20,0"/>

            <CheckBox Content="Auto-odświeżanie" 
              IsChecked="{Binding IsAutoRefreshEnabled}" 
              VerticalAlignment="Center" Margin="0,0,10,0"/>

            <TextBlock VerticalAlignment="Center"/>
            <Slider Minimum="1" Maximum="10" Width="100" 
            Value="{Binding RefreshInterval}" 
            IsSnapToTickEnabled="True" TickFrequency="1" 
            VerticalAlignment="Center" Margin="5,0"/>
            <TextBlock Text="{Binding RefreshInterval, StringFormat={}{0} s}" VerticalAlignment="Center" Width="30"/>
        </StackPanel>

        <DataGrid Grid.Row="1" Grid.Column="0"
                  ItemsSource="{Binding ProcessesView}" 
                  SelectedItem="{Binding SelectedProcess}"
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                <DataGridTextColumn Header="Nazwa" Binding="{Binding Name}" Width="200"/>
                <DataGridTextColumn Header="Pamięć" Binding="{Binding MemoryInMB}" Width="100"/>
                <DataGridTextColumn Header="Priorytet" Binding="{Binding Priority}" Width="100"/>
                <DataGridTextColumn Header="Wątki" Binding="{Binding ThreadCount}" Width="60"/>
            </DataGrid.Columns>
        </DataGrid>

        <GridSplitter Grid.Row="1" Grid.Column="1" Width="5" HorizontalAlignment="Stretch" />

        <ContentControl Grid.Row="1" Grid.Column="2">
            <vm:ProcessDetails xmlns:vm="clr-namespace:ProcessMonitor.Views"/>
        </ContentControl>

        <StackPanel Grid.Row="2" Grid.ColumnSpan="3" Orientation="Horizontal" Margin="0,10,0,0">
            <TextBlock Text="Zaznaczony proces: "/>
            <TextBlock Text="{Binding SelectedProcess.Name}" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>
